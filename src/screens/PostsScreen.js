import React from 'react';
import { StyleSheet, Text } from 'react-native';
import Wrapper from '../components/Wrapper';

export default class PostsScreen extends React.Component {
    render() {
        return (
            <Text>PostsScreen.js</Text>
        )
    }
}
